---
apiVersion: v1
kind: Service
metadata:
  name: taskflow-backend
spec:
  type: NodePort
  selector:
    app: taskflow-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
      nodePort: 30080
